{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window",
  "windows": ["main", "quick-add"],
  "permissions": ["core:default", "core:window:allow-hide", "notification:default"]
}
